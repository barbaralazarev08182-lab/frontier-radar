import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Saved · Frontier Radar" };

export default function SavedPage() {
  return (
    <PagePlaceholder
      title="Saved"
      description="收藏的条目与笔记（计划在后续阶段接入收藏能力）。"
    />
  );
}
