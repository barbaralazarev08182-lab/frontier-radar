import { SavedLibrary } from "./saved-library";
import "./saved-editorial-archive-v5.css";

export const metadata = { title: "Saved · Frontier Radar" };

export default function SavedPage() {
  return <SavedLibrary />;
}
