import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Idea Lab · Frontier Radar" };

export default function IdeaLabPage() {
  return (
    <PagePlaceholder
      title="Idea Lab"
      description="从条目沉淀可执行灵感（计划在后续阶段接入灵感管理）。"
    />
  );
}
